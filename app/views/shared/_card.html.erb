
<%= link_to book_path(book), style: "text-decoration: none;" do %>
<div class="card grid-item" style="<%= width %>">
  <div class="growth">
    <p>+ 3,6 %</p>
    <p>€ 3,53</p>
  </div>

  <div class="d-flex">
    <div class="image-upoad justify-content-between">
      <img src="https://mpd-biblio-covers.imgix.net/9781250847959.jpg?" alt="book-cover" height="230px">
    </div>

    <div class="book-info">
      <h3><%= book.title %></h3>
      <small><%= book.user.email %></small>
      <% if book.description.length > 150 %>
        <p><%= book.description.first(150) %>...</p>
      <% else %>
        <p><%= book.description%></p>
      <% end %>
    </div>
  </div>

  <div class="info-bar">
        <p>Queer Author</p>
        <P>Cerebral</P>
        <p>Classic</p>
  </div>
</div>
<% end %>
